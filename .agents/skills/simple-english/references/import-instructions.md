# Local import delta for `simple-english`

This file documents local modifications only.

## Local modifications

- Updated `SKILL.md` frontmatter `description` so this skill applies by default
  to all writing, while still including STE keyword cues for discoverability.

## Re-apply steps after upstream sync

1. Open `SKILL.md` frontmatter `description`.
2. Ensure the description states this skill applies by default to all writing
   unless I ask for a different style.
3. Keep STE keyword cues in the description for discoverability.
